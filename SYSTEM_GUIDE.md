# РУКОВОДСТВО ПО СИСТЕМЕ КОНТРОЛЯ ПОДРЯДЧИКОВ

## ОГЛАВЛЕНИЕ
1. [Общая архитектура](#общая-архитектура)
2. [Роли пользователей](#роли-пользователей)
3. [Объекты строительства](#объекты-строительства)
4. [Справочник работ (Work Templates)](#справочник-работ-work-templates)
5. [Работы на объектах](#работы-на-объектах)
6. [Жизненный цикл работ](#жизненный-цикл-работ)
7. [Права доступа](#права-доступа)
8. [Связи между сущностями](#связи-между-сущностями)

---

## ОБЩАЯ АРХИТЕКТУРА

Система представляет собой платформу для управления строительными объектами и работами на них. Основная концепция:

```
ОБЪЕКТ СТРОИТЕЛЬСТВА (Object)
  └── РАБОТА (Work)
       └── Назначена ПОДРЯДЧИКУ (Contractor)
```

**Основные сущности:**
- **Объекты** — строительные площадки (дома, корпуса, здания)
- **Работы** — конкретные задачи на объектах
- **Справочник работ** — библиотека типовых работ (шаблоны)
- **Пользователи** — заказчики, подрядчики, администраторы

---

## РОЛИ ПОЛЬЗОВАТЕЛЕЙ

В системе существует 3 основные роли:

### 1. **client** (Заказчик)
- Создаёт объекты строительства
- Назначает работы подрядчикам
- Видит все работы на своих объектах
- Принимает или отклоняет выполненные работы
- Управляет списком своих подрядчиков

### 2. **contractor** (Подрядчик)
- Видит только работы, назначенные ему
- Может менять статус своих работ
- Добавляет фото и материалы к работам
- Отчитывается о выполнении
- Видит только объекты, где у него есть работы

### 3. **admin** (Администратор)
- Полный доступ ко всем данным
- Может выполнять действия за любую роль
- Управляет пользователями и организациями
- Управляет справочником работ

---

## ОБЪЕКТЫ СТРОИТЕЛЬСТВА

### Что такое объект?
Объект — это физическое место, где ведутся работы (здание, корпус, участок дороги, сооружение и т.д.).

**Примеры объектов:**
- "ЖК Солнечный, корпус 3"
- "Торговый центр Мега, секция А"
- "Реконструкция школы №15"
- "Паркинг подземный, уровень -1"

### Структура данных объекта:
```
objects:
  - id (уникальный идентификатор)
  - title (название объекта)
  - address (адрес)
  - description (описание)
  - client_id (ID заказчика-владельца)
  - status (active/paused/completed)
  - created_at, updated_at
```

### Логика работы с объектами:

#### Создание объекта:
1. Заказчик нажимает "Создать объект"
2. Заполняет форму:
   - **Название** (обязательно) — например, "ЖК Солнечный, корпус 3"
   - **Адрес** (обязательно) — например, "г. Москва, ул. Ленина, 15"
   - **Описание** (опционально) — дополнительная информация
3. Объект создаётся в статусе `active`
4. Система автоматически привязывает `client_id = user_id` (владелец объекта)

#### Просмотр объектов:
**Заказчик** видит:
- Все объекты, где `client_id = user_id` (свои объекты)
- Список отображается в виде карточек или таблицы
- Показывается прогресс работ, количество работ, статус

**Подрядчик** видит:
- Только объекты, где у него есть назначенные работы
- Не видит объекты других заказчиков
- Запрос: `SELECT DISTINCT objects WHERE object_id IN (works где contractor_id = user_id)`

**Админ** видит:
- Все объекты в системе без ограничений

#### Статусы объекта:
- **active** — объект активен, работы ведутся
- **paused** — объект временно приостановлен
- **completed** — объект завершён

**Примечание:** Статус объекта устанавливается вручную заказчиком. Система также может вычислять динамический статус на основе работ:
- Если все работы выполнены → "Работы завершены"
- Если есть активные работы → "Работы начаты"
- Если все работы в статусе pending → "Запланировано"

#### Редактирование объекта:
- Может Заказчик (владелец)
- Может Админ
- Подрядчик НЕ может редактировать

**Что можно редактировать:**
- Название
- Адрес
- Описание
- Статус (active/paused/completed)

#### Удаление объекта:
- Может Заказчик (владелец)
- Может Админ
- При удалении объекта удаляются все его работы (каскадное удаление)
- Рекомендуется переводить объекты в статус `completed` вместо удаления

---

## СПРАВОЧНИК РАБОТ (Work Templates)

### Что это?
Справочник работ — это библиотека типовых строительных работ с их характеристиками. Это шаблоны, из которых потом создаются конкретные работы на объектах.

**Зачем нужен справочник?**
- Ускоряет создание работ (не нужно каждый раз заполнять описание, материалы)
- Стандартизирует процессы (одинаковое описание для одинаковых работ)
- Содержит нормативные ссылки (СНиП, ГОСТ)
- Содержит точки контроля качества

**Примеры записей справочника:**
- "Возведение кирпичных стен" (код: КС-01, категория: Каменные работы)
- "Монтаж электропроводки скрытой" (код: ЭМ-03, категория: Электромонтажные работы)
- "Укладка керамической плитки" (код: ОТ-12, категория: Отделочные работы)

### Структура данных:
```
work_templates:
  - id (уникальный идентификатор)
  - code (код работы, например "КС-01")
  - title (название работы)
  - description (подробное описание)
  - category (категория работы)
  - normative_ref (ссылка на нормативы: СНиП, ГОСТ и т.д.)
  - material_types (типы используемых материалов)
  - control_points (точки контроля качества в формате JSON)
  - created_at
```

### Категории работ:
Стандартные категории строительных работ:
- **Общестроительные работы**
- **Земляные работы**
- **Каменные работы**
- **Бетонные работы**
- **Монтаж конструкций**
- **Кровельные работы**
- **Отделочные работы**
- **Электромонтажные работы**
- **Сантехнические работы**
- **Вентиляция и кондиционирование**

### Точки контроля (control_points):
Это массив JSON с описанием критических проверок для работы.

**Пример:**
```json
[
  {
    "stage": "Подготовка",
    "checks": ["Проверка основания", "Разметка осей"]
  },
  {
    "stage": "Основные работы",
    "checks": ["Контроль толщины слоя", "Проверка вертикальности"]
  },
  {
    "stage": "Приёмка",
    "checks": ["Визуальный осмотр", "Замеры отклонений"]
  }
]
```

### Логика использования справочника:

#### 1. Просмотр справочника:
- Доступен всем авторизованным пользователям
- GET запрос возвращает список всех шаблонов
- Шаблоны отсортированы по категориям и названиям

#### 2. Добавление новой записи в справочник:
**Кто может:** Заказчик, Админ

**Сценарий:**
1. Пользователь открывает страницу "Справочник работ"
2. Нажимает кнопку "Добавить тип работы"
3. Заполняет форму:
   - **Код работы** (обязательно) — уникальный код, например "КС-01"
   - **Название** (обязательно) — например "Возведение кирпичных стен"
   - **Категория** (выбор из списка) — например "Каменные работы"
   - **Описание** (опционально) — подробное описание технологии
   - **Нормативные документы** (опционально) — СНиП 3.03.01-87, ГОСТ 530-2012
   - **Типы материалов** (опционально) — Кирпич керамический М150, раствор М75
   - **Точки контроля** (опционально) — массив JSON с этапами проверок
4. Нажимает "Сохранить"
5. Запись появляется в справочнике

#### 3. Использование при создании работы:
Когда Заказчик создаёт работу на объекте, он может:

**Вариант А: Создание из шаблона**
1. Заказчик нажимает "Добавить работу из справочника"
2. Выбирает шаблон (например, "Возведение кирпичных стен")
3. Система автоматически подставляет:
   - Название (из шаблона)
   - Описание (из шаблона)
   - Материалы (из шаблона)
   - Точки контроля (из шаблона)
4. Заказчик может отредактировать любые поля
5. Заказчик дополнительно указывает:
   - Конкретизирует название (например, "Возведение кирпичных стен, 3 этаж, секция А")
   - Выбирает подрядчика
   - Указывает плановые даты начала и окончания
6. Работа создаётся с полем `template_id = ID шаблона`

**Вариант Б: Создание вручную**
1. Заказчик создаёт работу без выбора шаблона
2. Заполняет все поля вручную
3. Поле `template_id` остаётся NULL

**Связь:**
```
work_templates (шаблон)
       ↓ template_id
works (конкретная работа на объекте)
```

#### 4. Редактирование шаблона:
**Кто может:** Только Админ

**Важно:** При редактировании шаблона существующие работы НЕ изменяются. Изменения применяются только к новым работам, созданным после редактирования.

#### 5. Удаление шаблона:
**Кто может:** Только Админ

**Важно:** При удалении шаблона существующие работы НЕ удаляются. У них просто остаётся `template_id` (связь опциональная).

---

## РАБОТЫ НА ОБЪЕКТАХ

### Что такое работа?
Работа — это конкретная задача для подрядчика на объекте.

**Примеры работ:**
- "Возведение стен 3 этажа, секция А" (на объекте "ЖК Солнечный, корпус 3")
- "Монтаж электрики в квартирах 301-310" (на объекте "ЖК Солнечный, корпус 3")
- "Укладка плитки в холле 1 этажа" (на объекте "ЖК Солнечный, корпус 3")

### Структура данных работы:
```
works:
  - id (уникальный идентификатор)
  - title (название работы)
  - description (описание)
  - object_id (к какому объекту относится)
  - contractor_id (ID подрядчика-исполнителя, может быть NULL)
  - contractor_organization_id (ID организации подрядчика, если есть)
  - template_id (ID шаблона из справочника, опционально)
  - status (статус работы: pending/in_progress/on_review/completed/rejected)
  - work_status (дублирующий статус для совместимости)
  - start_date (фактическая дата начала)
  - end_date (фактическая дата окончания)
  - planned_start_date (плановая дата начала)
  - planned_end_date (плановая дата окончания)
  - completion_percentage (процент выполнения 0-100)
  - photos (массив JSON с фотографиями)
  - materials (массив JSON с использованными материалами)
  - created_at, updated_at
```

### Логика создания работы:

#### Сценарий 1: Создание работы вручную
1. Заказчик открывает объект
2. Нажимает кнопку "Добавить работу"
3. Заполняет форму:
   - **Название работы** (обязательно) — например, "Монтаж окон 3 этаж"
   - **Описание** (опционально) — дополнительные детали
   - **Подрядчик** (опционально) — выбирает из списка своих подрядчиков
   - **Плановая дата начала** (опционально)
   - **Плановая дата окончания** (опционально)
4. Нажимает "Создать"
5. Работа создаётся в статусе `pending` (запланирована)
6. Поле `template_id` = NULL

#### Сценарий 2: Создание работы из шаблона
1. Заказчик открывает объект
2. Нажимает кнопку "Добавить работу из справочника"
3. Открывается модальное окно со списком шаблонов из `work_templates`
4. Заказчик выбирает шаблон (например, "Возведение кирпичных стен")
5. Система автоматически заполняет поля:
   - **Название** — из шаблона (title)
   - **Описание** — из шаблона (description)
   - **Материалы** — из шаблона (material_types)
   - **Точки контроля** — из шаблона (control_points)
   - **template_id** — ID выбранного шаблона
6. Заказчик дополняет/корректирует:
   - Конкретизирует название (например, добавляет "3 этаж, секция А")
   - Выбирает подрядчика
   - Указывает плановые даты
7. Нажимает "Создать"
8. Работа создаётся в статусе `pending`

#### Назначение/переназначение подрядчика:
- При создании работы подрядчик **может быть НЕ назначен** (`contractor_id = NULL`)
- Заказчик может позже назначить подрядчика:
  1. Открыть работу
  2. Нажать "Назначить исполнителя"
  3. Выбрать подрядчика из списка
  4. Подрядчик получает уведомление
- Заказчик может **переназначить** работу на другого подрядчика:
  1. Открыть работу
  2. Нажать "Сменить исполнителя"
  3. Выбрать нового подрядчика
  4. Статус работы сбрасывается на `pending`
  5. Новый подрядчик получает уведомление

---

## ЖИЗНЕННЫЙ ЦИКЛ РАБОТ

### Статусы работы:
1. **pending** (Запланирована) — работа создана, но ещё не начата
2. **in_progress** (В работе) — подрядчик начал выполнение
3. **on_review** (На проверке) — подрядчик завершил, ждёт приёмки заказчиком
4. **completed** (Выполнена) — работа принята заказчиком
5. **rejected** (Отклонена) — работа не принята, нужны доработки

### Диаграмма переходов:

```
pending → in_progress → on_review → completed
                            ↓
                        rejected → in_progress
```

### Таблица переходов:

| Переход | Кто выполняет | Действие | Что происходит |
|---------|---------------|----------|----------------|
| `pending` → `in_progress` | Подрядчик | "Начать работу" | Фиксируется `start_date = today` |
| `in_progress` → `on_review` | Подрядчик | "Отправить на проверку" | Фиксируется `end_date = today`, `completion_percentage = 100` |
| `on_review` → `completed` | Заказчик, Админ | "Принять работу" | Работа закрывается |
| `on_review` → `rejected` | Заказчик, Админ | "Отклонить работу" | Указывается причина, работа возвращается |
| `rejected` → `in_progress` | Подрядчик | "Взять в работу" | Подрядчик исправляет замечания |

### Детальная логика переходов:

#### 1. Подрядчик начинает работу (pending → in_progress)

**Условия:**
- Работа в статусе `pending`
- Текущий пользователь — назначенный подрядчик (`contractor_id = user_id`)

**Действия:**
1. Подрядчик открывает работу в своём списке
2. Видит кнопку "Начать работу"
3. Нажимает кнопку
4. Система выполняет:
   ```sql
   UPDATE works 
   SET status = 'in_progress', 
       work_status = 'in_progress',
       start_date = CURRENT_DATE,
       updated_at = NOW()
   WHERE id = work_id
   ```
5. Подрядчик видит уведомление: "Работа начата"
6. Заказчик получает уведомление: "Подрядчик [ИМЯ] начал работу [НАЗВАНИЕ]"

**Интерфейс для подрядчика:**
- Работа переходит из списка "Запланированные" в "В работе"
- Появляется возможность добавлять фото и материалы
- Можно обновлять процент выполнения

---

#### 2. Подрядчик завершает работу (in_progress → on_review)

**Условия:**
- Работа в статусе `in_progress`
- Текущий пользователь — назначенный подрядчик

**Действия:**
1. Подрядчик открывает работу
2. Добавляет фотографии выполненной работы (опционально):
   - Нажимает "Добавить фото"
   - Загружает изображения
   - Система сохраняет в поле `photos` (JSON)
3. Добавляет использованные материалы (опционально):
   - Нажимает "Добавить материалы"
   - Заполняет: название, количество, единица измерения, стоимость
   - Система сохраняет в поле `materials` (JSON)
4. Указывает процент выполнения (по умолчанию 100%)
5. Нажимает кнопку "Отправить на проверку"
6. Система выполняет:
   ```sql
   UPDATE works 
   SET status = 'on_review', 
       work_status = 'on_review',
       end_date = CURRENT_DATE,
       completion_percentage = 100,
       updated_at = NOW()
   WHERE id = work_id
   ```
7. Подрядчик видит уведомление: "Работа отправлена на проверку"
8. Заказчик получает уведомление: "Работа [НАЗВАНИЕ] готова к приёмке"

**Интерфейс для подрядчика:**
- Работа переходит в список "На проверке"
- Кнопки редактирования становятся неактивными

---

#### 3. Заказчик принимает работу (on_review → completed)

**Условия:**
- Работа в статусе `on_review`
- Текущий пользователь — заказчик (владелец объекта) или админ

**Действия:**
1. Заказчик получает уведомление о готовности работы
2. Открывает работу
3. Просматривает:
   - Описание работы
   - Фотографии (если подрядчик добавил)
   - Использованные материалы
   - Точки контроля (если работа создана из шаблона)
4. Если всё соответствует требованиям — нажимает "Принять работу"
5. Система показывает подтверждение: "Вы уверены, что хотите принять работу? Это действие нельзя отменить."
6. Заказчик подтверждает
7. Система выполняет:
   ```sql
   UPDATE works 
   SET status = 'completed', 
       work_status = 'completed',
       updated_at = NOW()
   WHERE id = work_id
   ```
8. Заказчик видит уведомление: "Работа принята"
9. Подрядчик получает уведомление: "Работа [НАЗВАНИЕ] принята заказчиком"

**Интерфейс:**
- Работа переходит в список "Завершённые"
- Все кнопки редактирования исчезают (только просмотр)
- На карточке объекта увеличивается счётчик выполненных работ

---

#### 4. Заказчик отклоняет работу (on_review → rejected)

**Условия:**
- Работа в статусе `on_review`
- Текущий пользователь — заказчик или админ

**Действия:**
1. Заказчик открывает работу
2. Видит несоответствие требованиям (плохое качество, не те материалы, не выполнены точки контроля)
3. Нажимает кнопку "Отклонить работу"
4. Открывается модальное окно "Причина отклонения":
   - Поле для текста (обязательное) — например, "Не выдержана толщина слоя штукатурки. Требуется переделать участок 3 этажа."
   - Кнопки: "Отменить" / "Отклонить"
5. Заказчик заполняет причину и нажимает "Отклонить"
6. Система выполняет:
   ```sql
   UPDATE works 
   SET status = 'rejected', 
       work_status = 'rejected',
       description = CONCAT(description, '\n\nПричина отклонения: ', rejection_reason),
       updated_at = NOW()
   WHERE id = work_id
   ```
   *(Причина может сохраняться в отдельное поле или добавляться в description)*
7. Заказчик видит уведомление: "Работа отклонена"
8. Подрядчик получает уведомление: "Работа [НАЗВАНИЕ] отклонена. Причина: [ТЕКСТ]"

**Интерфейс для подрядчика:**
- Работа переходит в список "Отклонённые"
- Отображается причина отклонения красным текстом
- Появляется кнопка "Взять в работу"

---

#### 5. Подрядчик дорабатывает отклонённую работу (rejected → in_progress)

**Условия:**
- Работа в статусе `rejected`
- Текущий пользователь — назначенный подрядчик

**Действия:**
1. Подрядчик открывает работу
2. Читает причину отклонения
3. Нажимает кнопку "Взять в работу"
4. Система выполняет:
   ```sql
   UPDATE works 
   SET status = 'in_progress', 
       work_status = 'in_progress',
       updated_at = NOW()
   WHERE id = work_id
   ```
5. Подрядчик видит уведомление: "Работа взята на доработку"
6. Подрядчик исправляет недостатки
7. Снова отправляет на проверку (см. пункт 2)

**Важно:** Цикл `on_review → rejected → in_progress` может повторяться несколько раз, пока работа не будет принята.

---

### Ограничения и правила:

#### Изменение данных работы:

**Заказчик:**
- ✅ Может редактировать в ЛЮБОМ статусе:
  - Название работы
  - Описание
  - Плановые даты (`planned_start_date`, `planned_end_date`)
  - Переназначать подрядчика
- ❌ НЕ может менять фактические даты (`start_date`, `end_date`)
- ❌ НЕ может добавлять фото/материалы (это право подрядчика)

**Подрядчик:**
- ✅ Может добавлять фото и материалы в статусах: `in_progress`, `rejected`
- ✅ Может обновлять `completion_percentage`
- ❌ НЕ может редактировать название, описание
- ❌ НЕ может менять плановые даты
- ❌ НЕ может редактировать в статусах: `on_review`, `completed`

**Админ:**
- ✅ Может редактировать всё в любом статусе

#### Удаление работы:
- ✅ Может удалить Заказчик (владелец объекта)
- ✅ Может удалить Админ
- ❌ Работу в статусе `completed` удалить нельзя (рекомендуется архивировать, а не удалять)

#### Процент выполнения:
- По умолчанию: 
  - `pending` = 0%
  - `in_progress` = обновляется подрядчиком вручную
  - `on_review` / `completed` = 100%
- Подрядчик может обновлять процент в любой момент в статусе `in_progress`

---

## ПРАВА ДОСТУПА

### Матрица прав доступа:

| Действие | Заказчик (client) | Подрядчик (contractor) | Админ (admin) |
|----------|-------------------|------------------------|---------------|
| **ОБЪЕКТЫ** |
| Создать объект | ✅ | ❌ | ✅ |
| Просмотреть объекты | ✅ (только свои) | ✅ (где есть работы) | ✅ (все) |
| Редактировать объект | ✅ (только свои) | ❌ | ✅ |
| Удалить объект | ✅ (только свои) | ❌ | ✅ |
| **РАБОТЫ** |
| Создать работу | ✅ (на своих объектах) | ❌ | ✅ |
| Просмотреть работы | ✅ (на своих объектах) | ✅ (только свои) | ✅ (все) |
| Редактировать работу | ✅ (название, описание, даты) | ⚠️ (только фото/материалы/%) | ✅ (всё) |
| Назначить подрядчика | ✅ | ❌ | ✅ |
| Начать работу (pending → in_progress) | ❌ | ✅ (свои) | ✅ |
| Отправить на проверку (in_progress → on_review) | ❌ | ✅ (свои) | ✅ |
| Принять работу (on_review → completed) | ✅ | ❌ | ✅ |
| Отклонить работу (on_review → rejected) | ✅ | ❌ | ✅ |
| Взять в работу после отклонения (rejected → in_progress) | ❌ | ✅ (свои) | ✅ |
| Удалить работу | ✅ | ❌ | ✅ |
| **СПРАВОЧНИК РАБОТ** |
| Просмотреть справочник | ✅ | ✅ | ✅ |
| Добавить шаблон | ✅ | ❌ | ✅ |
| Редактировать шаблон | ❌ | ❌ | ✅ |
| Удалить шаблон | ❌ | ❌ | ✅ |

---

## СВЯЗИ МЕЖДУ СУЩНОСТЯМИ

### Схема связей:

```
users (пользователи)
  ├── role: 'client' / 'contractor' / 'admin'
  └── onboarding_completed (для клиентов)

objects (объекты)
  ├── client_id → users.id (владелец объекта)
  └── Содержит несколько works

works (работы)
  ├── object_id → objects.id (на каком объекте)
  ├── contractor_id → users.id (кто исполняет, может быть NULL)
  ├── contractor_organization_id → organizations.id (организация подрядчика, опционально)
  └── template_id → work_templates.id (из какого шаблона создана, опционально)

work_templates (справочник работ)
  └── Используется для создания works через template_id

client_contractors (связь заказчик-подрядчик)
  ├── client_id → users.id
  └── contractor_id → users.id
  └── Список подрядчиков, с которыми работает заказчик
```

### Правило каскадного доступа:

**Для Заказчика:**
1. Пользователь видит объекты, где `objects.client_id = user_id`
2. Пользователь видит работы, где `works.object_id IN (мои объекты)`
3. Может назначать только подрядчиков из списка `client_contractors`, где `client_id = user_id`

**Для Подрядчика:**
1. Пользователь видит работы, где `works.contractor_id = user_id`
2. Пользователь видит объекты, где `objects.id IN (объекты с моими работами)`
3. НЕ видит другие работы на тех же объектах (только свои)

**Для Админа:**
- Видит всё без ограничений

---

## ДОПОЛНИТЕЛЬНЫЕ ФИЧИ

### 1. Фотографии работ

**Назначение:** Подрядчик фотографирует выполненную работу для подтверждения качества.

**Структура данных:**
```json
works.photos = [
  {
    "url": "https://storage.example.com/photo1.jpg",
    "uploaded_at": "2025-01-15T14:30:00Z",
    "description": "Фото после укладки плитки, вид общий"
  },
  {
    "url": "https://storage.example.com/photo2.jpg",
    "uploaded_at": "2025-01-15T14:32:00Z",
    "description": "Крупный план швов"
  }
]
```

**Логика:**
- Добавляются подрядчиком в статусах `in_progress` или `rejected`
- Хранятся в поле `photos` (JSONB)
- Заказчик видит фотографии при приёмке работы

---

### 2. Материалы

**Назначение:** Подрядчик отчитывается о фактически использованных материалах.

**Структура данных:**
```json
works.materials = [
  {
    "name": "Кирпич керамический М150",
    "quantity": 1000,
    "unit": "шт",
    "cost": 50000
  },
  {
    "name": "Раствор цементный М75",
    "quantity": 2.5,
    "unit": "м³",
    "cost": 15000
  }
]
```

**Логика:**
- Добавляются подрядчиком в процессе работы
- Хранятся в поле `materials` (JSONB)
- Используются для:
  - Контроля расхода материалов
  - Финансовой отчётности
  - Сравнения плана и факта

---

### 3. Уведомления

Система отправляет уведомления при следующих событиях:

| Событие | Кто получает | Текст уведомления |
|---------|--------------|-------------------|
| Работа назначена подрядчику | Подрядчик | "Вам назначена работа: [НАЗВАНИЕ] на объекте [ОБЪЕКТ]" |
| Подрядчик начал работу | Заказчик | "Подрядчик [ИМЯ] начал работу: [НАЗВАНИЕ]" |
| Работа отправлена на проверку | Заказчик | "Работа [НАЗВАНИЕ] готова к приёмке" |
| Работа принята | Подрядчик | "Работа [НАЗВАНИЕ] принята заказчиком" |
| Работа отклонена | Подрядчик | "Работа [НАЗВАНИЕ] отклонена. Причина: [ТЕКСТ]" |
| Приближается дедлайн (за 3 дня) | Подрядчик | "Внимание! Работа [НАЗВАНИЕ] должна быть завершена через 3 дня" |
| Дедлайн просрочен | Заказчик, Подрядчик | "Работа [НАЗВАНИЕ] просрочена на [N] дней" |

---

### 4. Аналитика и отчёты

#### Для объекта:
- **Общее количество работ**
- **Работы по статусам:**
  - Запланировано: X
  - В работе: X
  - На проверке: X
  - Выполнено: X
  - Отклонено: X
- **Прогресс выполнения:**
  ```
  Прогресс = (Сумма completion_percentage всех работ) / (Количество работ)
  ```
- **Просроченные работы:**
  - Работы, где `CURRENT_DATE > planned_end_date` и статус != `completed`
- **Список активных подрядчиков на объекте**

#### Для заказчика:
- **Все объекты:** количество работ, прогресс по каждому
- **Загрузка подрядчиков:** сколько работ у каждого, в каких статусах
- **Просроченные работы:** общий список по всем объектам
- **Выполнение в срок:** процент работ, выполненных до дедлайна

---

## ТИПИЧНЫЙ СЦЕНАРИЙ ИСПОЛЬЗОВАНИЯ СИСТЕМЫ

### Полный цикл от регистрации до приёмки работы:

#### Этап 1: Регистрация и настройка (Заказчик)
1. Пользователь регистрируется → `role = 'client'`
2. Проходит онбординг → `onboarding_completed = true`
3. Добавляет своих подрядчиков:
   - Вводит email подрядчика
   - Система отправляет приглашение
   - Подрядчик регистрируется с `role = 'contractor'`
   - Создаётся связь в `client_contractors`

#### Этап 2: Создание объекта и работ (Заказчик)
4. Заказчик создаёт объект:
   - Название: "ЖК Солнечный, корпус 3"
   - Адрес: "г. Москва, ул. Ленина, 15"
   - Объект в статусе `active`
5. Заказчик создаёт работы на объекте:

   **Вариант А:** Создаёт вручную
   - "Монтаж окон 3 этаж"
   - Описание: "Установить 20 окон ПВХ"
   - Подрядчик: Иванов И.И.
   - Плановые даты: 01.02.2025 - 10.02.2025

   **Вариант Б:** Создаёт из справочника
   - Выбирает шаблон "Возведение кирпичных стен"
   - Система подставляет описание, материалы, точки контроля
   - Конкретизирует: "Возведение кирпичных стен, 3 этаж, секция А"
   - Назначает подрядчика: Петров П.П.
   - Плановые даты: 15.02.2025 - 28.02.2025

#### Этап 3: Выполнение работ (Подрядчик)
6. Подрядчик получает уведомление о назначенной работе
7. Открывает работу в своём списке
8. Нажимает "Начать работу" → статус `in_progress`
9. В процессе работы:
   - Добавляет фото (например, фото кладки после каждого дня)
   - Добавляет материалы (кирпич 5000 шт, раствор 10 м³)
   - Обновляет процент выполнения (25%, 50%, 75%)
10. Завершает работу:
    - Добавляет финальные фото
    - Устанавливает процент 100%
    - Нажимает "Отправить на проверку" → статус `on_review`

#### Этап 4: Приёмка работы (Заказчик)
11. Заказчик получает уведомление
12. Открывает работу
13. Просматривает фотографии, описание, материалы
14. **Сценарий А:** Работа выполнена качественно
    - Нажимает "Принять работу" → статус `completed`
    - Подрядчик получает уведомление

15. **Сценарий Б:** Есть замечания
    - Нажимает "Отклонить работу"
    - Указывает причину: "Швы не заполнены раствором на участке 3-5 метров"
    - Статус → `rejected`
    - Подрядчик получает уведомление с причиной
    - Подрядчик нажимает "Взять в работу"
    - Исправляет замечания
    - Снова отправляет на проверку
    - Заказчик принимает работу → статус `completed`

---

## ТЕХНИЧЕСКИЕ ДЕТАЛИ РЕАЛИЗАЦИИ

### Backend API Endpoints:

#### Объекты (`/backend/objects`)
- `GET ?client_id=X` — список объектов заказчика
- `GET ?contractor_id=X` — объекты подрядчика (где у него есть работы)
- `GET ?id=X` — детали объекта
- `POST` — создать объект
  ```json
  {
    "title": "ЖК Солнечный, корпус 3",
    "address": "г. Москва, ул. Ленина, 15",
    "description": "Жилой 10-этажный дом",
    "status": "active"
  }
  ```
- `PUT ?id=X` — обновить объект
- `DELETE ?id=X` — удалить объект

#### Работы (`/backend/works`)
- `GET ?object_id=X` — список работ объекта
- `GET ?id=X` — детали работы
- `POST` — создать работу
  ```json
  {
    "object_id": 123,
    "title": "Монтаж окон 3 этаж",
    "description": "Установить 20 окон ПВХ",
    "contractor_id": 456,
    "template_id": 78,
    "planned_start_date": "2025-02-01",
    "planned_end_date": "2025-02-10"
  }
  ```
- `PUT ?id=X` — обновить работу
- `DELETE ?id=X` — удалить работу

#### Справочник работ (`/backend/work-types`)
- `GET` — список всех шаблонов
- `POST` — создать шаблон
  ```json
  {
    "code": "КС-01",
    "title": "Возведение кирпичных стен",
    "category": "Каменные работы",
    "description": "Кладка стен из керамического кирпича...",
    "normative_ref": "СНиП 3.03.01-87",
    "material_types": "Кирпич М150, раствор М75",
    "control_points": [
      {"stage": "Подготовка", "checks": ["Проверка основания"]},
      {"stage": "Приёмка", "checks": ["Визуальный осмотр"]}
    ]
  }
  ```
- `PUT ?id=X` — обновить шаблон
- `DELETE ?id=X` — удалить шаблон

---

### Database Schema:

```sql
-- Пользователи
users:
  id SERIAL PRIMARY KEY
  phone VARCHAR(20) UNIQUE NOT NULL
  role VARCHAR(20) NOT NULL -- 'client' / 'contractor' / 'admin'
  full_name VARCHAR(255)
  onboarding_completed BOOLEAN DEFAULT FALSE
  created_at TIMESTAMP DEFAULT NOW()

-- Объекты
objects:
  id SERIAL PRIMARY KEY
  title VARCHAR(255) NOT NULL
  address TEXT
  description TEXT
  client_id INT NOT NULL REFERENCES users(id) -- владелец объекта
  status VARCHAR(20) DEFAULT 'active' -- active/paused/completed
  created_at TIMESTAMP DEFAULT NOW()
  updated_at TIMESTAMP DEFAULT NOW()

-- Работы
works:
  id SERIAL PRIMARY KEY
  title VARCHAR(255) NOT NULL
  description TEXT
  object_id INT NOT NULL REFERENCES objects(id) ON DELETE CASCADE
  contractor_id INT REFERENCES users(id) -- может быть NULL (не назначен)
  contractor_organization_id INT REFERENCES organizations(id)
  template_id INT REFERENCES work_templates(id) -- опционально
  status VARCHAR(20) DEFAULT 'pending' -- pending/in_progress/on_review/completed/rejected
  work_status VARCHAR(20) DEFAULT 'planned' -- дубль для совместимости
  start_date DATE -- фактическая дата начала
  end_date DATE -- фактическая дата окончания
  planned_start_date DATE -- плановая дата начала
  planned_end_date DATE -- плановая дата окончания
  completion_percentage INT DEFAULT 0
  photos JSONB DEFAULT '[]'
  materials JSONB DEFAULT '[]'
  created_at TIMESTAMP DEFAULT NOW()
  updated_at TIMESTAMP DEFAULT NOW()

-- Справочник работ
work_templates:
  id SERIAL PRIMARY KEY
  code VARCHAR(50) NOT NULL UNIQUE
  title TEXT NOT NULL
  description TEXT
  category VARCHAR(255)
  normative_ref VARCHAR(255)
  material_types TEXT
  control_points JSONB -- [{"stage": "...", "checks": [...]}]
  created_at TIMESTAMP DEFAULT NOW()

-- Связь заказчик-подрядчик
client_contractors:
  id SERIAL PRIMARY KEY
  client_id INT NOT NULL REFERENCES users(id)
  contractor_id INT NOT NULL REFERENCES users(id)
  added_at TIMESTAMP DEFAULT NOW()
  UNIQUE(client_id, contractor_id)
```

---

## ЗАКЛЮЧЕНИЕ

Это полное описание логики системы Реестра Объектов и Работ **без сущности проектов**. 

**Ключевые отличия от версии с проектами:**
- Объекты напрямую принадлежат заказчику (`client_id`)
- Нет промежуточного уровня "проект"
- Иерархия: Заказчик → Объект → Работа

По этому документу можно:
- Воссоздать функционал с нуля
- Понять бизнес-процессы
- Написать документацию для пользователей
- Обучить новых разработчиков
- Перенести на вашу инфраструктуру

Если нужны уточнения — дополню! 🚀
