# РУКОВОДСТВО ПО СИСТЕМЕ КОНТРОЛЯ ПОДРЯДЧИКОВ

## ОГЛАВЛЕНИЕ
1. [Общая архитектура](#общая-архитектура)
2. [Роли пользователей](#роли-пользователей)
3. [Регистрация и организации](#регистрация-и-организации)
4. [Объекты строительства](#объекты-строительства)
5. [Справочник работ (Work Templates)](#справочник-работ-work-templates)
6. [Работы на объектах](#работы-на-объектах)
7. [Жизненный цикл работ](#жизненный-цикл-работ)
8. [Права доступа](#права-доступа)
9. [Связи между сущностями](#связи-между-сущностями)

---

## ОБЩАЯ АРХИТЕКТУРА

Система представляет собой платформу для управления строительными объектами и работами на них через организации. Основная концепция:

```
ОРГАНИЗАЦИЯ ЗАКАЗЧИКА (Organization, type=client)
  └── ОБЪЕКТ СТРОИТЕЛЬСТВА (Object)
       └── РАБОТА (Work)
            └── Назначена ОРГАНИЗАЦИИ ПОДРЯДЧИКА (Organization, type=contractor)
                 └── Сотрудники подрядчика (Users через user_organizations)
```

**Ключевые принципы:**
- Объекты привязаны к **организациям заказчика**
- Работы назначаются **организациям подрядчика**
- Пользователи являются **сотрудниками организаций**
- Пользователь видит данные тех организаций, в которых он состоит

---

## РОЛИ ПОЛЬЗОВАТЕЛЕЙ

### Роли в системе:

#### 1. Системная роль (users.role):
- **client** — пользователь организации-заказчика
- **contractor** — пользователь подрядной организации
- **admin** — системный администратор (особая роль, полный доступ к системе)

#### 2. Роль в организации (user_organizations.role):
- **admin** — администратор организации
- **employee** — сотрудник организации

### Различия ролей:

**Системный администратор (users.role = 'admin'):**
- Полный доступ ко ВСЕЙ системе
- Управление всеми пользователями через Админ-панель
- Редактирование справочника работ (work_templates)
- Просмотр и управление всеми организациями, объектами, работами
- Может выполнять действия от имени любой роли
- НЕ привязан к конкретной организации
- Доступ к специальным разделам: "Админ-панель", "Справочник"

**Администратор организации (user_organizations.role = 'admin'):**
- Создатель организации (первый пользователь)
- Может редактировать данные СВОЕЙ организации
- Может приглашать новых сотрудников в СВОЮ организацию
- Заполняет информацию об организации при онбординге (шаги 2-4)
- НЕ является системным админом (users.role = 'client' или 'contractor')
- Видит только данные своих организаций

**Сотрудник организации (user_organizations.role = 'employee'):**
- Приглашённый пользователь
- НЕ может редактировать данные организации
- НЕ может приглашать новых сотрудников
- При онбординге заполняет ТОЛЬКО персональные данные (шаг 1)
- Имеет доступ к объектам и работам организации (просмотр, работа с задачами)
- Видит только данные своих организаций

---

## СИСТЕМНЫЙ АДМИНИСТРАТОР (users.role = 'admin')

Системный администратор — это особая роль с полным доступом ко всем функциям системы и данным.

### Отличия от других ролей:

| Характеристика | Системный админ (users.role = 'admin') | Admin организации (user_organizations.role = 'admin') | Client / Contractor |
|----------------|---------------------------------------|------------------------------------------------------|---------------------|
| Доступ к данным | ВСЕ организации, объекты, работы | Только СВОИ организации | Только СВОИ организации |
| Админ-панель | ✅ Полный доступ | ❌ Нет доступа | ❌ Нет доступа |
| Справочник работ | ✅ Редактирование | ❌ Только просмотр | ❌ Только просмотр |
| Управление пользователями | ✅ Все пользователи системы | ✅ Только пользователи своих организаций | ❌ Нет доступа |
| Создание организаций | ✅ Любые типы | ✅ Только свои | ✅ Только свои |
| Удаление данных | ✅ Любые данные | ❌ Только свои | ❌ Только свои |

### Специальные разделы для системного администратора:

#### 1. Админ-панель пользователей

**Путь:** `/admin/users` (доступен только users.role = 'admin')

**Функционал:**
- **Просмотр всех пользователей системы:**
  - Таблица со всеми пользователями
  - Колонки: ID, ФИО, Телефон, Email, Роль (client/contractor/admin), Статус онбординга
  - Поиск по ФИО, телефону, email
  - Фильтрация по роли (client/contractor/admin)
  - Фильтрация по статусу онбординга (завершён/не завершён)

- **Просмотр детальной информации о пользователе:**
  - Персональные данные (ФИО, телефон, email, должность)
  - Организации, в которых состоит пользователь (с ролями в каждой)
  - История действий (логи активности)
  - Статус онбординга

- **Редактирование пользователей:**
  - Изменение роли пользователя (client → contractor → admin)
  - Изменение персональных данных
  - Сброс онбординга (пользователь должен пройти заново)
  - Активация/деактивация пользователя

- **Управление связями пользователь-организация:**
  - Добавление пользователя в организацию (с указанием роли: admin/employee)
  - Удаление пользователя из организации
  - Изменение роли пользователя в организации (admin ↔ employee)

- **Удаление пользователей:**
  - Мягкое удаление (пользователь помечается как удалённый, данные остаются)
  - Жёсткое удаление (ОСТОРОЖНО: удаляются все связи и данные пользователя)
  - При удалении проверяется: есть ли объекты/работы, созданные пользователем

#### 2. Справочник работ

**Путь:** `/admin/work-templates` (доступен только users.role = 'admin')

**Функционал:**
- **Просмотр справочника:**
  - Таблица всех работ из справочника
  - Колонки: Код, Название, Категория, Дата создания
  - Поиск по названию, коду, описанию
  - Фильтрация по категориям

- **Создание новой работы:**
  - Форма с полями:
    - Код работы (обязательно, уникальный, например "ВД-01")
    - Название (обязательно)
    - Описание (опционально, текстовое поле)
    - Категория (выбор из списка)
    - Нормативные документы (текстовое поле: СНиПы, ГОСТы)
    - Материалы (текстовое поле)
    - Контрольные точки (JSONB, список точек контроля качества)
  - Валидация уникальности кода
  - Сохранение → запись добавляется в work_templates

- **Редактирование существующей работы:**
  - Открытие формы с заполненными данными
  - Изменение любых полей (кроме id)
  - Сохранение → обновление записи в work_templates

- **Удаление работы:**
  - Проверка: используется ли работа в таблице works (есть ли works.template_id на эту запись)
  - Если используется → показать ошибку "Невозможно удалить, запись используется в X работах"
  - Если НЕ используется → удалить запись из work_templates
  - Подтверждение удаления (модальное окно)

#### 3. Управление всеми организациями

**Путь:** `/admin/organizations` (доступен только users.role = 'admin')

**Функционал:**
- Просмотр всех организаций (заказчики и подрядчики)
- Редактирование данных любой организации
- Просмотр сотрудников организации
- Удаление организаций (с проверкой зависимостей)

#### 4. Просмотр всех объектов и работ

**Функционал:**
- Системный админ видит ВСЕ объекты и работы всех организаций
- Может редактировать/удалять любые объекты и работы
- Может выполнять действия от имени заказчика или подрядчика

### Создание системного администратора:

⚠️ **КРИТИЧНО:** Системный администратор создаётся ТОЛЬКО через прямое изменение БД:

```sql
-- Создание системного администратора
INSERT INTO users (phone, role, full_name, onboarding_completed)
VALUES ('+79991234567', 'admin', 'Системный Администратор', true);
```

**Особенности:**
- У системного администратора НЕТ организаций (нет записей в user_organizations)
- `onboarding_completed = true` (не проходит онбординг)
- Доступ ко всем разделам системы
- В навигации отображаются дополнительные пункты: "Админ-панель", "Справочник"

---

## РЕГИСТРАЦИЯ И ОРГАНИЗАЦИИ

### 1. Общая логика регистрации

⚠️ **КРИТИЧНО:**
- В системе **самостоятельно регистрируются ТОЛЬКО заказчики**
- Подрядчики добавляются **ТОЛЬКО по приглашению** от заказчика
- Системные администраторы создаются ТОЛЬКО через БД

### 2. Типы организаций

| Тип | Название | Кто создаёт | Описание |
|-----|----------|-------------|----------|
| **client** | Заказчик | Сам пользователь при регистрации | Создаёт объекты, назначает работы |
| **contractor** | Подрядная организация | Заказчик через "Пригласить подрядчика" | Выполняет работы на объектах |

---

### 3. Регистрация заказчика

#### Процесс:
1. **Пользователь проходит регистрацию** (указывает телефон, получает код)
2. **Система автоматически:**
   - Создаёт пользователя с `role = 'client'`
   - Создаёт организацию с `type = 'client'`
   - Связывает пользователя с организацией через `user_organizations` с `role = 'admin'`
3. **Пользователь попадает в онбординг** (4 шага)

#### Онбординг заказчика (4 шага):

**Шаг 1: Персональные данные**
- ФИО
- Email (опционально)
- Должность (опционально)

**Шаг 2: Данные организации**
⚠️ Доступен ТОЛЬКО администратору организации
- Название организации (обязательно)
- ИНН (обязательно)
- КПП (опционально)
- ОГРН (опционально)

**Шаг 3: Адреса и контакты**
⚠️ Доступен ТОЛЬКО администратору организации
- Юридический адрес
- Фактический адрес
- Телефон организации
- Email организации

**Шаг 4: Реквизиты**
⚠️ Доступен ТОЛЬКО администратору организации
- БИК банка
- Название банка
- Расчётный счёт
- Корреспондентский счёт
- ФИО директора
- Должность директора

После завершения онбординга:
- `users.onboarding_completed = true`
- Пользователь получает доступ к основному функционалу

---

### 4. Приглашение сотрудника в организацию заказчика

#### Процесс:
1. **Администратор организации** открывает настройки организации
2. Нажимает **"Пригласить сотрудника"**
3. Заполняет форму:
   - **Номер телефона** (обязательно)
4. Нажимает "Отправить приглашение"
5. **Система создаёт:**
   - Запись в `organization_invites`:
     - `organization_id` = ID текущей организации
     - `phone` = указанный номер телефона
     - `status` = 'pending'
     - `token` = уникальный токен приглашения
     - `expires_at` = дата истечения (например, +7 дней)
6. **На номер отправляется SMS/уведомление** с приглашением

#### Принятие приглашения:
1. **Приглашённый пользователь** регистрируется/авторизуется по номеру телефона
2. **Система проверяет:**
   - Есть ли для этого номера активное приглашение в `organization_invites`
3. **Если приглашение найдено:**
   - Создаётся запись в `user_organizations`:
     - `user_id` = ID нового пользователя
     - `organization_id` = ID организации из приглашения
     - `role` = **'employee'** (приглашённые становятся сотрудниками, НЕ админами)
   - Приглашение помечается как использованное (`status = 'accepted'`)
   - Пользователю присваивается `role = 'client'` (если организация type=client) или `role = 'contractor'` (если type=contractor)
4. **Пользователь попадает в онбординг:**
   - ⚠️ **Только Шаг 1** (персональные данные)
   - Шаги 2-4 (организация) **НЕ отображаются**, т.к.:
     - Пользователь уже привязан к существующей организации
     - У пользователя роль `employee`, а не `admin`

---

### 5. Раздел "Подрядчики" у заказчика

#### Назначение:
В личном кабинете заказчика есть раздел **"Подрядчики"**, где отображаются все подрядные организации, с которыми он сотрудничает.

#### Добавление подрядчика:

1. **Заказчик** открывает раздел "Подрядчики"
2. Нажимает **"Пригласить подрядчика"**
3. Заполняет форму:
   - **ИНН организации** (обязательно)
   - **Название организации** (обязательно)
   - **Номер телефона первого сотрудника** (обязательно) — он станет администратором подрядной организации
4. Нажимает "Пригласить"

5. **Система выполняет проверку по ИНН:**

   **Вариант А: Организация с таким ИНН уже существует**
   - Система показывает: "Организация [НАЗВАНИЕ] с ИНН [ИНН] уже существует. Присоединить её?"
   - Заказчик подтверждает
   - Система создаёт связь в `client_contractors`:
     - `client_id` = ID организации заказчика
     - `contractor_id` = ID найденной организации
   - Подрядная организация появляется в списке подрядчиков заказчика

   **Вариант Б: Организация с таким ИНН НЕ существует**
   - Система создаёт новую организацию:
     - `type = 'contractor'`
     - `name` = из формы
     - `inn` = из формы
     - `created_by` = ID текущего пользователя (заказчика)
   - Система создаёт приглашение в `organization_invites`:
     - `organization_id` = ID созданной организации
     - `phone` = указанный номер телефона
     - `invited_by` = ID заказчика
     - `token` = уникальный токен
     - `expires_at` = +7 дней
   - Система создаёт связь в `client_contractors`:
     - `client_id` = ID организации заказчика
     - `contractor_id` = ID новой организации
   - **На номер телефона отправляется приглашение**

6. **Приглашённый администратор подрядчика:**
   - Регистрируется/авторизуется по номеру телефона
   - Система находит приглашение
   - Создаётся запись в `user_organizations` с `role = 'admin'` ⚠️ **Важно:** Первый приглашённый в подрядную организацию становится администратором
   - Пользователю присваивается `role = 'contractor'`
   - Попадает в онбординг (4 шага), где заполняет данные подрядной организации

---

### 6. Таблицы в БД

#### organizations
```
organizations:
  - id
  - name (название организации)
  - inn (ИНН, уникальный)
  - type ('client' / 'contractor')
  - kpp, ogrn (опционально)
  - legal_address, actual_address
  - phone, email
  - bik, bank_name, payment_account, correspondent_account
  - director_name, director_position
  - status ('active' / 'inactive')
  - created_by (user_id создателя)
  - created_at, updated_at
```

#### user_organizations (связь пользователь-организация)
```
user_organizations:
  - id
  - user_id → users.id
  - organization_id → organizations.id
  - role ('admin' / 'employee')
    - 'admin': создатель организации или первый приглашённый в подрядную организацию
    - 'employee': приглашённый сотрудник
  - created_at
  UNIQUE(user_id, organization_id)
```

#### organization_invites (приглашения в организации)
```
organization_invites:
  - id
  - organization_id → organizations.id
  - phone (номер телефона приглашённого)
  - email (опционально)
  - invited_by → users.id (кто пригласил)
  - token (уникальный токен приглашения)
  - status ('pending' / 'accepted' / 'expired')
  - expires_at (дата истечения)
  - created_at
```

#### client_contractors (связь заказчик-подрядчик)
```
client_contractors:
  - id
  - client_id → organizations.id (организация-заказчик)
  - contractor_id → organizations.id (организация-подрядчик)
  - added_at
  UNIQUE(client_id, contractor_id)
```

---

### 7. Примеры сценариев

#### Сценарий 1: Регистрация заказчика
```
1. Иванов И.И. регистрируется по телефону +79991234567
2. Система создаёт:
   - Пользователя: users.role = 'client'
   - Организацию: "ООО Строй-Инвест", type = 'client'
   - Связь: user_organizations (role = 'admin')
3. Иванов проходит онбординг (4 шага), заполняет данные организации
4. onboarding_completed = true
5. Иванов получает доступ к системе
```

#### Сценарий 2: Заказчик приглашает сотрудника
```
1. Иванов (администратор "ООО Строй-Инвест") приглашает Сидорова
2. Указывает телефон +79997654321
3. Система создаёт приглашение в organization_invites
4. Сидоров регистрируется по телефону +79997654321
5. Система находит приглашение, связывает Сидорова с "ООО Строй-Инвест"
6. Создаётся user_organizations с role = 'employee'
7. Сидоров проходит онбординг (ТОЛЬКО ШАГ 1 — персональные данные)
8. Сидоров становится сотрудником (employee) организации "ООО Строй-Инвест"
9. Сидоров НЕ может редактировать данные организации, НЕ может приглашать других
```

#### Сценарий 3: Заказчик добавляет нового подрядчика
```
1. Иванов (администратор "ООО Строй-Инвест") открывает "Подрядчики"
2. Нажимает "Пригласить подрядчика"
3. Заполняет:
   - ИНН: 7707654321
   - Название: "ООО Монтаж-Строй"
   - Телефон: +79995556677
4. Система проверяет ИНН — организации нет
5. Система создаёт:
   - Организацию "ООО Монтаж-Строй" (type = 'contractor')
   - Приглашение на +79995556677
   - Связь в client_contractors
6. Петров регистрируется по телефону +79995556677
7. Система привязывает Петрова к "ООО Монтаж-Строй" (role = 'admin')
8. Петров проходит онбординг (4 шага), заполняет данные подрядчика
9. Петров становится администратором "ООО Монтаж-Строй"
10. Иванов может назначать работы организации "ООО Монтаж-Строй"
```

#### Сценарий 4: Заказчик добавляет существующего подрядчика
```
1. Иванов хочет добавить подрядчика
2. Вводит ИНН 7707999888
3. Система находит организацию "ООО Электромонтаж" с этим ИНН
4. Показывает: "Организация уже существует. Присоединить?"
5. Иванов подтверждает
6. Система создаёт связь в client_contractors
7. "ООО Электромонтаж" появляется в списке подрядчиков Иванова
8. Иванов может назначать работы этой организации
```

---

## ОБЪЕКТЫ СТРОИТЕЛЬСТВА

### Что такое объект?
Объект — это физическое место, где ведутся работы (здание, корпус, участок дороги, сооружение и т.д.).

**Критично:** Объекты принадлежат **организациям-заказчикам**, а не пользователям.

### Структура данных объекта:
```
objects:
  - id
  - title (название объекта)
  - address (адрес)
  - description (описание)
  - client_id → organizations.id (организация-заказчик)
  - status ('active' / 'paused' / 'completed')
  - created_at, updated_at
```

### Логика работы:

#### Создание объекта:
1. Администратор организации-заказчика нажимает "Создать объект"
2. Заполняет:
   - Название (обязательно)
   - Адрес (обязательно)
   - Описание (опционально)
3. Объект создаётся с `client_id` = ID организации пользователя

#### Просмотр объектов:
**Для заказчика:**
```sql
SELECT objects.*
FROM objects
JOIN user_organizations uo ON objects.client_id = uo.organization_id
WHERE uo.user_id = current_user_id
```

**Для подрядчика:**
```sql
SELECT DISTINCT objects.*
FROM objects
JOIN works ON objects.id = works.object_id
JOIN user_organizations uo ON works.contractor_organization_id = uo.organization_id
WHERE uo.user_id = current_user_id
```

---

## СПРАВОЧНИК РАБОТ (Work Templates)

Справочник работ — это централизованная база типовых строительных работ, которая используется для создания работ на объектах.

### Структура:
```
work_templates:
  - id
  - code (код работы, например "ВД-01", "КР-05")
  - title (название)
  - description (описание)
  - category (категория)
  - normative_ref (нормативы: СНиПы, ГОСТы)
  - material_types (используемые материалы)
  - control_points (точки контроля качества, JSONB)
  - created_at
  - updated_at
```

### Категории работ:
- Общестроительные работы
- Земляные работы
- Каменные работы
- Бетонные работы
- Монтаж конструкций
- Кровельные работы
- Отделочные работы
- Электромонтажные работы
- Сантехнические работы
- Вентиляция и кондиционирование

### Доступ к справочнику:

⚠️ **КРИТИЧНО:** 
- **Редактирование справочника доступно ТОЛЬКО системным администраторам** (users.role = 'admin')
- Заказчики и подрядчики могут ТОЛЬКО просматривать и использовать записи из справочника
- Системный админ управляет справочником через раздел **"Справочник"** в навигации

### Функционал для системного администратора:

**Раздел "Справочник" (доступен только admin):**
- Просмотр всех записей в справочнике работ
- Фильтрация по категориям
- Поиск по названию, коду, описанию
- Создание новых записей:
  - Код работы (обязательно, уникальный)
  - Название (обязательно)
  - Описание (опционально)
  - Категория (выбор из списка)
  - Нормативные документы (СНиПы, ГОСТы)
  - Материалы (типы используемых материалов)
  - Контрольные точки (JSONB с описанием точек контроля)
- Редактирование существующих записей
- Удаление записей (если не используются в работах)

**Логика удаления:**
- Нельзя удалить запись из справочника, если она используется в работах (есть `works.template_id` на эту запись)
- При попытке удаления система проверяет наличие связей
- Если есть связи → показывается ошибка "Невозможно удалить, запись используется в X работах"

### Использование справочника:

⚠️ **КРИТИЧНО:** 
- Каждая работа на объекте ОБЯЗАТЕЛЬНО связана с записью из справочника через `template_id`
- При создании работы заказчик выбирает категорию, затем конкретную работу из справочника
- Название и описание из справочника подтягиваются автоматически, но можно переопределить
- Нормативные документы отображаются в табе "Нормативная документация" работы

---

## РАБОТЫ НА ОБЪЕКТАХ

### Структура:
```
works:
  - id
  - title (название)
  - description (описание)
  - object_id → objects.id
  - contractor_organization_id → organizations.id (организация-подрядчик)
  - template_id → work_templates.id (ОБЯЗАТЕЛЬНО)
  - status ('created' / 'awaiting_start' / 'in_progress' / 'delayed' / 'on_review' / 'completed' / 'rejected')
  - actual_start_date (фактическая дата начала, заполняется при уведомлении о начале)
  - actual_end_date (фактическая дата окончания)
  - planned_start_date (плановая дата начала)
  - planned_end_date (плановая дата окончания)
  - volume (объём работ, число)
  - unit (единица измерения: м², м³, шт., компл. и т.д.)
  - completion_percentage (0-100, для отчётов)
  - created_at, updated_at
```

### Создание и редактирование работ:

**Кто может:** Сотрудники организации-заказчика (admin и employee)

**UI-логика:**
- Работы отображаются в виде раскрывающихся/скрывающихся секций (аккордеон)
- В свёрнутом состоянии видны: название работы, статус, подрядчик, даты
- При клике работа раскрывается с полной формой редактирования
- Можно редактировать несколько работ одновременно

**Создание работы:**
1. Сотрудник организации-заказчика открывает объект
2. Нажимает кнопку "+" (добавить работу) внизу списка
3. Новая работа добавляется в свёрнутом состоянии, автоматически раскрывается
4. Заполняет форму:
   - **Категория работ** (обязательно) — выбор из категорий справочника
   - **Вид работ** (обязательно) — выбор из work_templates по выбранной категории
   - **Объём работ** — числовое значение (можно 0)
   - **Единица измерения** — выбор (м², м³, шт., компл. и т.д.)
   - **Плановое начало** / **Плановое окончание** — даты из календаря
   - **Подрядчик** (обязательно) — выбор из списка подрядчиков организации
5. При выборе "Вид работ" название и описание подтягиваются из шаблона
6. Работа связывается со справочником через `template_id`
7. Кнопка "Дублировать" — копирует работу со всеми параметрами
8. Кнопка "Сохранить работы" (одна на весь объект) — сохраняет все изменения

**Редактирование:**
- Клик на свёрнутую работу → раскрывается форма
- Изменение любых полей (кроме подрядчика и template_id после создания)
- Клик на стрелку или другую работу → работа сворачивается
- Кнопка "Отмена" — отменяет все несохранённые изменения



---

## РАЗДЕЛЫ РАБОТЫ (Табы внутри работы)

После создания работы и назначения подрядчика, работа открывается с несколькими разделами (табами):

### 1. Чат
**Назначение:** Основной канал коммуникации между заказчиком и подрядчиком по конкретной работе

**Функции (текущие):**
- Текстовые сообщения между сотрудниками заказчика и подрядчика
- Прикрепление фото, документов, файлов к сообщениям
- История переписки по работе

**💡 Идеи для будущего:**
- **Кнопка "Уведомить о начале работ"** (видна только подрядчику, только в статусе `created`)
  - При нажатии: статус меняется на `in_progress`, фиксируется `actual_start_date`
  - В чат автоматически добавляется системное сообщение "Подрядчик уведомил о начале работ"
- **Кнопка "Запросить приёмку работ"** (видна только подрядчику, только в статусе `in_progress`)
  - При нажатии: статус меняется на `on_review`
  - В чат добавляется системное сообщение "Подрядчик запросил приёмку работ"
- **Кнопки "Принять работы" / "Отклонить работы"** (видны только заказчику, только в статусе `on_review`)
  - "Принять" → статус меняется на `completed`, фиксируется `actual_end_date`
  - "Отклонить" → статус меняется на `rejected`, требуется причина (текст)

**Структура сообщений:**
```
work_messages:
  - id
  - work_id → works.id
  - user_id → users.id (отправитель)
  - text (текст сообщения)
  - attachments (JSONB: [{url, type, name, size}])
  - created_at
```

---

### 2. Журнал
**Назначение:** Ведение исполнительной документации и отчётов подрядчика

**Функции (текущие):**
- Подрядчик создаёт отчёты о выполненных работах
- Каждый отчёт содержит:
  - Дата отчёта
  - Описание выполненных работ
  - Объём выполненных работ (число)
  - Фотографии (прикрепление нескольких фото)
- Заказчик просматривает отчёты подрядчика
- История всех отчётов по работе в хронологическом порядке

**Структура:**
```
work_reports:
  - id
  - work_id → works.id
  - created_by → users.id (подрядчик)
  - report_date (дата отчёта)
  - description (описание выполненных работ)
  - volume_completed (объём выполненных работ, число)
  - photos (JSONB: [{url, description}])
  - created_at
```

**💡 Идеи для будущего:**
- Подписи заказчика на отчётах
- Автоматическое формирование актов выполненных работ
- Записи о приёмке скрытых работ

---

### 3. Нормативная документация
**Назначение:** Хранение нормативов, СНиПов, ГОСТов по данной работе

**Функции (текущие):**
- Просмотр нормативных документов, применимых к работе
- Данные подтягиваются из `work_templates.normative_ref` (из справочника)
- Список нормативов отображается в виде ссылок или текста

**Структура:**
```
work_templates.normative_ref (текстовое поле или JSONB)
  - Содержит список применимых СНиПов, ГОСТов, других нормативов
  - Отображается в табе "Нормативная документация"
```

**💡 Идеи для будущего:**
- Загрузка дополнительных нормативных документов (файлы PDF)
- Ссылки на внешние источники
- Контрольные точки по нормативам

---

### 4. Сметы
**Назначение:** Хранение версий сметной документации по работе

**Функции (текущие):**
- Загрузка файлов смет (PDF, Excel и т.д.)
- Версионность смет (можно загрузить несколько версий)
- Актуальная версия сметы отмечается как последняя загруженная
- Просмотр и скачивание загруженных смет
- **Редактирование ЗАПРЕЩЕНО** — только загрузка готовых файлов

**Структура:**
```
work_estimate_files:
  - id
  - work_id → works.id
  - version (номер версии: 1, 2, 3...)
  - file_url (ссылка на файл сметы)
  - file_name (название файла)
  - uploaded_by → users.id (кто загрузил)
  - uploaded_at
  - is_active (true для актуальной версии)
```

**Логика:**
- Заказчик или подрядчик загружает файл сметы
- Система создаёт новую версию (номер версии = MAX(version) + 1)
- Предыдущая версия остаётся в истории, но новая помечается `is_active = true`
- В табе отображаются все версии, актуальная выделена

---

### 5. Аналитика
**Назначение:** Визуализация прогресса работ на основе данных из сметы и отчётов подрядчика

**Функции (текущие):**
- **Данные из актуальной сметы:**
  - Плановый объём работ (из сметы)
  - Плановая стоимость (из сметы, если указана)
- **Данные из отчётов подрядчика (work_reports):**
  - Сумма всех `volume_completed` из отчётов = фактический объём выполненных работ
  - Процент выполнения = (сумма volume_completed / плановый объём из сметы) × 100%
- **Визуализация:**
  - График прогресса: плановый объём vs фактический объём
  - Процент выполнения работ (прогресс-бар)
  - Индикатор отставания (если фактический объём < планового по срокам)
- **Статистика по датам:**
  - Плановые даты начала/окончания (из `planned_start_date`, `planned_end_date`)
  - Фактическая дата начала (из `actual_start_date`)
  - Отклонение в днях (если есть просрочка)

**Расчёты:**
- Процент выполнения = `SUM(work_reports.volume_completed) / work.volume × 100%`
- Отставание = `CURRENT_DATE > planned_end_date AND status NOT IN ('completed')`
- Дней просрочки = `CURRENT_DATE - planned_end_date` (если есть отставание)

**💡 Идеи для будущего:**
- График выполнения по дням (динамика прогресса)
- Прогноз даты завершения на основе текущего темпа
- История изменения статусов
- Время, проведённое в каждом статусе

---

## СТАТУСЫ РАБОТ

⚠️ **КРИТИЧНО:** Статусы работ меняются автоматически на основе действий пользователей и системных проверок. Ручное изменение статуса НЕ предусмотрено.

### Текущие статусы:

#### 1. `created` — Работа создана
**Когда:** Заказчик создал работу, назначил подрядчика, сохранил

**Доступные действия:**
- Заказчик: редактирование параметров работы (название, объём, даты, подрядчик)
- Подрядчик: просмотр работы, общение в чате

**Как работает сейчас:**
- Работа остаётся в этом статусе до начала выполнения
- Переход в следующий статус: вручную (пока не реализовано автоматическое изменение)

**💡 Идея для будущего:** 
- Подрядчик нажимает кнопку "Уведомить о начале работ" → статус меняется на `in_progress`, фиксируется `actual_start_date`

---

#### 2. `in_progress` — Работа в процессе выполнения
**Когда:** Работа начата (статус установлен вручную или подрядчиком)

**Доступные действия:**
- Подрядчик: создание отчётов в журнале, загрузка документов, общение в чате
- Заказчик: просмотр отчётов подрядчика, аналитики, общение в чате

**Как работает сейчас:**
- Подрядчик отчитывается через "Журнал" (добавляет записи с объёмом выполненных работ)
- Система отображает прогресс в табе "Аналитика"
- Если `CURRENT_DATE > planned_end_date` → работа подсвечивается как просроченная (визуально)

**💡 Идеи для будущего:**
- Автоматический переход в статус `delayed` при просрочке
- Кнопка "Запросить приёмку работ" → переход в `on_review`

---

#### 3. `completed` — Работа завершена
**Когда:** Работа выполнена и принята (статус установлен вручную)

**Доступные действия:**
- Заказчик и подрядчик: просмотр истории, чат, журнал, документы (только чтение)
- Редактирование параметров работы ЗАПРЕЩЕНО

**Как работает сейчас:**
- Фиксируется `actual_end_date` (дата завершения)
- Работа отображается в списке завершённых работ
- Аналитика показывает итоговый результат

**💡 Идеи для будущего:**
- Автоматический переход из `on_review` при нажатии заказчиком "Принять работы"
- Автоматическое формирование акта выполненных работ

---

### 💡 Будущие статусы (не реализовано):

#### `delayed` — Работа с отставанием от графика
**Когда:** `CURRENT_DATE > planned_end_date` и работа в статусе `in_progress`

**Что будет:**
- Автоматическая подсветка работы красным цветом
- Уведомление заказчику о просрочке
- Отображение количества дней просрочки в аналитике

---

#### `on_review` — Работа на приёмке
**Когда:** Подрядчик нажал кнопку "Запросить приёмку работ"

**Что будет:**
- Системное сообщение в чате "Подрядчик запросил приёмку работ"
- Уведомление заказчику
- Кнопки для заказчика: "Принять работы" / "Отклонить работы"

---

#### `rejected` — Работа отклонена заказчиком
**Когда:** Заказчик нажал "Отклонить работы" (с указанием причины)

**Что будет:**
- Системное сообщение в чате с причиной отклонения
- Подрядчик устраняет замечания, может повторно запросить приёмку

---

### Текущая логика (как работает СЕЙЧАС):

1. **Создание:** Заказчик создаёт работу → статус `created`
2. **Начало:** Вручную меняется на `in_progress` (или автоматически в будущем)
3. **Выполнение:** Подрядчик отчитывается в журнале, система считает прогресс
4. **Просрочка:** Если `CURRENT_DATE > planned_end_date` → визуальная подсветка (статус НЕ меняется автоматически)
5. **Завершение:** Вручную меняется на `completed`

### Индикаторы статусов в UI (текущие):

- `created` — 🔵 Синий бейдж "Создана"
- `in_progress` — 🟢 Зелёный бейдж "В работе"
  - Если просрочена: 🔴 Красная подсветка + текст "Просрочена на X дней"
- `completed` — ✅ Серый бейдж "Завершена"

---

### Просмотр работ:

**Для заказчика (client):**
```sql
SELECT works.*
FROM works
JOIN objects ON works.object_id = objects.id
JOIN user_organizations uo ON objects.client_id = uo.organization_id
WHERE uo.user_id = current_user_id
```

**Для подрядчика (contractor):**
```sql
SELECT works.*
FROM works
JOIN user_organizations uo ON works.contractor_organization_id = uo.organization_id
WHERE uo.user_id = current_user_id
```

**Для системного администратора (admin):**
```sql
-- Системный админ видит ВСЕ работы без фильтрации
SELECT works.* FROM works;
```

---

## ЖИЗНЕННЫЙ ЦИКЛ РАБОТ

### Статусы:
1. **pending** — запланирована
2. **in_progress** — в работе
3. **on_review** — на проверке
4. **completed** — выполнена
5. **rejected** — отклонена

### Переходы:
```
pending → in_progress → on_review → completed
                            ↓
                        rejected → in_progress
```

### Кто может менять статусы:

| Переход | Кто | Проверка доступа |
|---------|-----|------------------|
| pending → in_progress | Любой сотрудник подрядной организации (admin/employee) | `works.contractor_organization_id IN (мои организации)` |
| in_progress → on_review | Любой сотрудник подрядной организации (admin/employee) | То же |
| on_review → completed | Любой сотрудник организации-заказчика (admin/employee) | `objects.client_id IN (мои организации)` |
| on_review → rejected | Любой сотрудник организации-заказчика (admin/employee) | То же |
| rejected → in_progress | Любой сотрудник подрядной организации (admin/employee) | `works.contractor_organization_id IN (мои организации)` |

⚠️ **Важно:** Работать с задачами (менять статусы работ) могут ВСЕ сотрудники организации, независимо от роли (admin/employee).

---

## ПРАВА ДОСТУПА

### Матрица прав:

| Действие | Admin орг-ции заказчика | Employee орг-ции заказчика | Admin орг-ции подрядчика | Employee орг-ции подрядчика | Системный админ |
|----------|-------------------------|----------------------------|--------------------------|------------------------------|-----------------|
| **ОРГАНИЗАЦИИ** |
| Создать организацию | ✅ (при регистрации) | ❌ | ❌ (только по приглашению) | ❌ | ✅ (любые типы) |
| Редактировать организацию | ✅ (свою) | ❌ | ✅ (свою) | ❌ | ✅ (любую) |
| Удалить организацию | ❌ | ❌ | ❌ | ❌ | ✅ |
| Пригласить сотрудника | ✅ | ❌ | ✅ | ❌ | ✅ |
| Добавить подрядчика | ✅ | ❌ | ❌ | ❌ | ✅ |
| **ОБЪЕКТЫ** |
| Создать объект | ✅ | ✅ | ❌ | ❌ | ✅ |
| Просмотреть объекты | ✅ (своих орг-ций) | ✅ (своих орг-ций) | ✅ (где есть работы) | ✅ (где есть работы) | ✅ (все) |
| Редактировать объект | ✅ | ✅ | ❌ | ❌ | ✅ (любой) |
| Удалить объект | ✅ | ❌ | ❌ | ❌ | ✅ |
| **РАБОТЫ** |
| Создать работу | ✅ | ✅ | ❌ | ❌ | ✅ |
| Просмотреть работы | ✅ (своих орг-ций) | ✅ (своих орг-ций) | ✅ (своих орг-ций) | ✅ (своих орг-ций) | ✅ (все) |
| Редактировать работу | ✅ | ✅ | ❌ | ❌ | ✅ (любую) |
| Удалить работу | ✅ | ❌ | ❌ | ❌ | ✅ |
| Начать/завершить работу | ✅ | ✅ | ✅ | ✅ | ✅ |
| Принять/отклонить работу | ✅ | ✅ | ❌ | ❌ | ✅ |
| **СПРАВОЧНИК РАБОТ** |
| Просмотр справочника | ✅ | ✅ | ✅ | ✅ | ✅ |
| Создать запись в справочнике | ❌ | ❌ | ❌ | ❌ | ✅ |
| Редактировать запись в справочнике | ❌ | ❌ | ❌ | ❌ | ✅ |
| Удалить запись из справочника | ❌ | ❌ | ❌ | ❌ | ✅ |
| **АДМИН-ПАНЕЛЬ** |
| Доступ к админ-панели пользователей | ❌ | ❌ | ❌ | ❌ | ✅ |
| Просмотр всех пользователей | ❌ | ❌ | ❌ | ❌ | ✅ |
| Редактирование пользователей | ❌ | ❌ | ❌ | ❌ | ✅ |
| Изменение роли пользователя | ❌ | ❌ | ❌ | ❌ | ✅ |
| Удаление пользователей | ❌ | ❌ | ❌ | ❌ | ✅ |

**Ключевые различия:**
- **Системный админ (users.role = 'admin'):** Полный доступ ко ВСЕМУ, включая админ-панель и справочник работ
- **Admin организации (user_organizations.role = 'admin'):** Может редактировать СВОЮ организацию, приглашать сотрудников
- **Employee организации (user_organizations.role = 'employee'):** Может работать с объектами и задачами, но НЕ может управлять организацией

---

## СВЯЗИ МЕЖДУ СУЩНОСТЯМИ

```
users (пользователи)
  ├── role: 'client' / 'contractor' / 'admin'
  └── Связаны с organizations через user_organizations

user_organizations
  ├── user_id → users.id
  ├── organization_id → organizations.id
  └── role: 'admin' / 'employee'
       - admin: создатель организации, может редактировать, приглашать
       - employee: приглашённый сотрудник, не может управлять организацией

organizations
  ├── type: 'client' / 'contractor'
  ├── Организации-заказчики владеют objects
  └── Организациям-подрядчикам назначаются works

objects
  ├── client_id → organizations.id
  └── Содержит works

works
  ├── object_id → objects.id
  ├── contractor_organization_id → organizations.id
  └── template_id → work_templates.id (ОБЯЗАТЕЛЬНО)

client_contractors (связь заказчик-подрядчик)
  ├── client_id → organizations.id
  └── contractor_id → organizations.id

organization_invites (приглашения)
  ├── organization_id → organizations.id
  └── phone (номер приглашённого)
```

---

## ЗАКЛЮЧЕНИЕ

Это полное описание системы с учётом **актуальной логики регистрации и организаций**.

**Ключевые особенности:**
- Только заказчики регистрируются самостоятельно
- Подрядчики добавляются по приглашению от заказчика
- **Роли в организациях:** 
  - `admin` — создатель организации или первый в подрядной организации (может редактировать, приглашать)
  - `employee` — приглашённые сотрудники (не могут редактировать организацию, не могут приглашать)
- Онбординг 4 шага: 
  - Шаг 1 (персональные данные) — для всех
  - Шаги 2-4 (данные организации) — только для администраторов (`role = 'admin'`)
- Объекты принадлежат организациям-заказчикам
- Работы назначаются организациям-подрядчикам
- Работы создаются ТОЛЬКО из справочника

По этому документу можно воссоздать функционал! 🚀